diplomska:
- bugs:
	- add snap to ifc, check how cursor interacts when drawing on IFC model

	extra features:
	- add advanced interface tools for drawing (grids, etc.)
	- prepare test case data

	scene editor:
	- add option to annotate geometries with properties (adding text properties or JSON objects into geometry properties)
	- add geojson import that imports geojson properties into a scene